# Psy Observer Web (Beta 3) — Python dependencies
# Install into a fresh venv: pip install -r requirements-observer.txt
fastapi>=0.110
uvicorn[standard]>=0.27
numpy>=1.26
pydantic>=2.0
httpx>=0.27
pytest>=8.0
