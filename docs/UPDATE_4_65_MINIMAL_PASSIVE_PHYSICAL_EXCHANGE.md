# Update 4.65 — Minimal Passive Physical Exchange

One-new-capability architecture + validation experiment.

Default-off `passive_physical_exchange_config` completes the existing 4.9
environmental-exchange → 4.8 `process_materials` relation so local
`env_material_field` can write `energy_reserve` under WAIT.

Does not implement 4.66. Does not compose Q. Does not force locomotion.
Coefficients are the existing 4.8/4.9 values.
